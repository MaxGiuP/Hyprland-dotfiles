# QuickMail validation builds are regenerable and can otherwise fill the
# RAM-backed /tmp mount. Clean stale Cargo output without touching source trees.
e /tmp/quickmail-*/target - - - 6h -
e /tmp/quickmail-*/tree/target - - - 6h -
e /tmp/quickmail-*/validation/target - - - 6h -
